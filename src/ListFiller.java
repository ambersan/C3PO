//import java.sql.*;
//import java.util.*;

public class ListFiller 
{
	//filling the list is the function and database side of the project
	
		/*

		//make a list to house the retrieved data
		List<Task> data =  new ArrayList<Task>();
	    
		//connect to the database
		//Connection conn = null;

	    try {
	    	
	    	//try to connect to the database
	        Class.forName(Driver);
	        conn = DriverManager.getConnection(URL,"root","");
	        String sql ="select * from //TODO LIST TABLE";
	        PreparedStatement Stmt = conn.prepareStatement(sql);
	        ResultSet rs = Stmt.executeQuery(sql);

	        //while there are values in the table
	        while(rs.next())
	        {
	        	
	        	//make a new task object 
		        Task event = new Task();
		        
		        //event.setDescription(rs.getString("description"));
		            
		        //add the event to the list of tasks
		        data.add(event);
	        }
	        
	        //close database connection
	        rs.close();
	        conn.close();

	    } catch (ClassNotFoundException ex) {
	        System.err.println(ex.getMessage());
	    }

	    return data;
	*/
}
